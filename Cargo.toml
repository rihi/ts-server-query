[package]
name = "TsServerQuery"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2"
tokio = { version = "1", features = ["io-util", "macros", "sync", "time"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt"] }
